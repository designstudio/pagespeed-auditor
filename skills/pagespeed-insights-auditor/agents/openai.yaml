interface:
  display_name: "PageSpeed Auditor"
  short_description: "Runs local Lighthouse and optional PSI follow-up"
  default_prompt: "Use $pagespeed-insights-auditor to run a local Lighthouse audit for this project, then map the biggest findings back to code changes."
